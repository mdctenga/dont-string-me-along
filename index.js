function findMissingNumber(argument) {
  // break argument into an argument array
  // iterate over the argument array
  // with a test array of 0 - 9, check against the argument array
    // if number exists in array, removed from test array
  // Ideally, in the end you'll end up with an array of numbers that you didn't find.
}
findMissingNumber(9899100101103104105);